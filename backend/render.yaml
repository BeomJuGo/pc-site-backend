services:
  - type: web
    name: pc-site-backend
    runtime: node
    buildCommand: npm ci
    startCommand: node --max-old-space-size=512 index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER
        value: "true"
