name: Daily Case Sync

on:
  schedule:
    # 매일 한국시간 오전 3시 25분 실행 → UTC 18:25 (전날)
    - cron: "25 18 * * *"
  workflow_dispatch:  # 수동 실행 버튼

jobs:
  sync-case:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: sync-case
      cancel-in-progress: false

    env:
      BACKEND_URL: https://pc-site-backend.onrender.com
      API_PATH: /api/sync-case
      PAGES: "8"

    steps:
      - name: Call backend to sync Case (with retries)
        shell: bash
        run: |
          set -e
          echo "POST $BACKEND_URL$API_PATH pages=$PAGES"

          for i in 1 2 3; do
            echo "Attempt $i..."
            http_code=$(curl -sS -o /tmp/resp.txt -w "%{http_code}" \
              -X POST "$BACKEND_URL$API_PATH" \
              -H "Content-Type: application/json" \
              -d "{\"pages\": ${PAGES}, \"ai\": true, \"force\": false}") || true

            echo "HTTP $http_code"
            cat /tmp/resp.txt || true

            if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
              echo "✅ Success"
              exit 0
            fi

            echo "⚠️ Failed (HTTP $http_code). Retrying in 20s..."
            sleep 20
          done

          echo "❌ All retries failed"
          exit 1
